<nav class="bg-white shadow-md p-5 sticky top-0 z-50">
  <div class="container mx-auto flex justify-between items-center">

    <!-- Brand Logo -->
    <a href="/"
      class="text-3xl font-extrabold tracking-wide text-red-600">Airbnb</a>

    <!-- Mobile Menu Button (Three Dots) -->
    <button id="menu-toggle" class="md:hidden text-gray-700 focus:outline-none">
      <i class="fas fa-ellipsis-v text-2xl"></i>
    </button>

    <!-- Desktop Navigation Links -->
    <div id="menu" class="hidden md:flex space-x-6">
      <a href="/home"
        class="nav-link <%= currentPage === 'Home' ? 'active' : '' %>">Home
        List</a>
      <a href="/favorites"
        class="nav-link <%= currentPage === 'Favorites' ? 'active' : '' %>">Favorites</a>
      <a href="/bookings"
        class="nav-link <%= currentPage === 'Bookings' ? 'active' : '' %>">Bookings</a>
      <a href="/host/host-home-list"
        class="nav-link <%= currentPage === 'host-homes' ? 'active' : '' %>">Host
        Home</a>
      <a href="/host/add-home"
        class="nav-link <%= currentPage === 'Add-Home' ? 'active' : '' %>">Add
        Home</a>
    </div>

    <!-- Mobile Dropdown Menu -->
    <div id="mobile-menu"
      class="absolute top-16 right-5 bg-white shadow-lg rounded-lg hidden md:hidden w-48">
      <a href="/home"
        class="mobile-link <%= currentPage === 'Home' ? 'active' : '' %>">Home
        List</a>
      <a href="/favorites"
        class="mobile-link <%= currentPage === 'Favorites' ? 'active' : '' %>">Favorites</a>
      <a href="/bookings"
        class="mobile-link <%= currentPage === 'Bookings' ? 'active' : '' %>">Bookings</a>
      <a href="/host/host-home-list"
        class="mobile-link <%= currentPage === 'host-homes' ? 'active' : '' %>">Host
        Home</a>
      <a href="/host/add-home"
        class="mobile-link <%= currentPage === 'Add-Home' ? 'active' : '' %>">Add
        Home</a>
    </div>
  </div>
</nav>

<!-- JavaScript for Mobile Menu Toggle -->
<script>
  document.getElementById("menu-toggle").addEventListener("click", () => {
    document.getElementById("mobile-menu").classList.toggle("hidden");
  });

  // Close menu if clicked outside
  document.addEventListener("click", (event) => {
    const menu = document.getElementById("mobile-menu");
    const toggleButton = document.getElementById("menu-toggle");
    
    if (!menu.contains(event.target) && !toggleButton.contains(event.target)) {
      menu.classList.add("hidden");
    }
  });
</script>

<!-- Tailwind CSS Styles -->
<style>
  .nav-link {
    position: relative;
    font-size: 1rem;
    font-weight: 500;
    color: #374151; /* Gray-700 */
    transition: color 0.3s ease-in-out;
  }

  .nav-link:hover {
    color: #ef4444; /* Red hover */
  }

  .nav-link::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -2px;
    width: 0%;
    height: 2px;
    background: #ef4444;
    transition: width 0.3s ease-in-out;
  }

  .nav-link:hover::after,
  .nav-link.active::after {
    width: 100%;
  }

  .nav-link.active {
    font-weight: bold;
    color: #ef4444;
  }

  /* Mobile Menu Styles */
  .mobile-link {
    display: block;
    padding: 12px 20px;
    font-size: 1rem;
    color: #374151;
    border-bottom: 1px solid #e5e7eb;
    transition: background 0.3s;
  }

  .mobile-link:hover,
  .mobile-link.active {
    background: #f87171;
    color: white;
  }
</style>
